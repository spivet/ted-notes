import head from './config-head';
import themeConfig from './config-theme';

export default {
    lang: 'en-US',
    base: '/ted-notes/',
    srcDir: './src',
    outDir: './dist',
    title: 'TED Notes',
    description: 'Lessons worth sharing!',
    head,
    themeConfig,
}
